name: quoteweaver

services:
  quoteweaver:
    build:
      context: .
      dockerfile: Dockerfile.jvm
      args:
        BUILD_COMMIT: "${BUILD_COMMIT}"
    image: localhost/quote-weaver:${BUILD_COMMIT}
    container_name: quote-weaver
    restart: unless-stopped
    environment:
      - BUILD_COMMIT=${BUILD_COMMIT}
    ports:
      - "8080:8080"
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "--fail",
          "--silent",
          "--show-error",
          "http://localhost:8080/api/ping",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - net

networks:
  net:
    driver: bridge
